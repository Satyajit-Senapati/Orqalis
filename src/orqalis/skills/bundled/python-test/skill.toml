id = "python-test"
version = "1.0.0"
description = "Python deterministic testing"
capabilities = ["pytest"]
applicable_when = ["python"]
required_tools = ["filesystem.read", "test.run"]
constraints = ["Preserve the accepted task contract"]
outputs = ["structured_result"]
