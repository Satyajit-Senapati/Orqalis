Inspect the scoped schema, persistence models, repository methods and migration history. Follow the existing database and migration framework. Keep persisted contracts and migrations consistent; preserve constraints, foreign keys, transaction boundaries and deterministic ordering. Use parameterized queries and indexes justified by actual query patterns. Explain compatibility and data-migration implications in the structured result.

Validate schema upgrades and affected persistence behavior only with approved commands against the configured disposable test database. This skill does not authorize connecting to production, reading credentials, dropping or resetting data, executing unapproved migrations, adding infrastructure, or altering acceptance criteria. Report actual migration and test evidence; state which checks could not run.
