id = "evidence-review"
version = "1.0.0"
description = "Evidence-backed acceptance review"
capabilities = ["evidence_review"]
applicable_when = []
required_tools = ["filesystem.read", "git.diff"]
constraints = ["Preserve the accepted task contract"]
outputs = ["structured_result"]
