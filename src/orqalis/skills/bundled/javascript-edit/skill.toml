id = "javascript-edit"
version = "1.0.0"
description = "Scoped JavaScript implementation following the existing module and test setup"
capabilities = ["javascript"]
applicable_when = ["javascript"]
required_tools = ["filesystem.read", "filesystem.write"]
constraints = ["Preserve the accepted task contract", "Use only granted tools and approved commands"]
outputs = ["structured_result", "source_references", "validation_evidence"]
